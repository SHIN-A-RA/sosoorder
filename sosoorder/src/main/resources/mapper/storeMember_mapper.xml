<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.soso.app.storeMember.mapper.StoreMemberMapper">
	
	<!-- 현재 등록된 회원정보 조회 -->
	<select id="getStoreMemberList" parameterType="storeMemberVO"
		resultType="storeMemberVO">
		SELECT *
		FROM memberAdmin
		order by phone
	</select>
	<!--회원정보 단건조회 -->
	<select id="getStoreMember" parameterType="storeMemberVO" resultType="storeMemberVO">
		select *
		FROM memberAdmin
		where
		phone = #{phone}
	</select>
	
	<!--회원정보 수정 -->
	<!-- <update id="StoreMemberUpdate" parameterType="storeMemberVO">
		UPDATE memberAdmin
		set
		storeId=#{storeId},
		memberNum=#{memberNum},
		phone=#{phone},
		delAddr=#{delAddr},
		point=#{point}
		where phone= #{phone}
	</update> -->
	<!--회원 삭제 -->
	<update id="StoreMemberDelete" parameterType="storeMemberVO">
		DELETE from memberAdmin where phone = ${phone}
	</update>
</mapper>